# dn-nitzan-devis

DreamNova proposal — isolated **nitzan** site.

`VITE_SITE=nitzan` is baked in `.env.production`. This deployment never shows the other client or the agency hub.

Build: `npm run build` (Nitro Vercel preset).
